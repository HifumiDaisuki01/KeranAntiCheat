# KeranAntiCheat v3.1.0 更新说明

本版为**独立代码审计后的结构性修复批次**，共核实并修复 8 项问题（KAC-01 ~ KAC-10）。
重点解决"检测器结构上无法触发"与"处罚链路形同虚设"两类严重缺陷。

## 修复清单

| 编号 | 严重度 | 问题 | 修复方式 |
|---|---|---|---|
| KAC-01 | P0 | `.gitignore` 裸写 `world/` 导致源码包 `check/world/` 从未入库 | 改为 `/world/`，7 个文件强制入库 |
| KAC-02 | P1 | `handleReport()` 仅校验 JSON 可解析，`{}` 即可绕过强制客户端门槛 | 新增 6 项字段校验 + 指纹复算自洽性校验 |
| KAC-03 | P1 | `AimCheck` 判据 2 阈值 250 越界，为数学死代码 | 阈值降至 140 并新增旋转方向反转率判据 |
| KAC-04 | P1 | 黑名单子串匹配导致 `impactful` 误报 | 改为词边界分词匹配，前缀族要求长度 ≥ 8 |
| KAC-05 | P1 | `BlinkCheck` 结构性无法触发（Paper 丢弃 + 阈值真空区） | 改为 per-tick 速度口径，阈值 3.0 → 1.6 格/tick |
| KAC-06 | P2 | 事件遍历空转（每次遍历全部 28 个检测器）+ 冗余权限查询 | 按事件类型预建索引 + 豁免判定 tick 缓存 |
| KAC-07 | P2 | `AutoClickerCheck` 把挖方块计入攻击 CPS | 攻击 CPS 改由 `EntityDamageByEntityEvent` 采集 |
| KAC-09 | P2 | `default-kick-vl` 形同虚设，自动处罚链路不可用 | 修正阈值回落语义（0 → 用默认，-1 → 显式关闭） |

> KAC-10（`isStoneLike` 使用 `contains`）经核实为**报告误判**，原代码已用 `equals`；
> 本次仍做防御性强化，全部改为精确匹配并补充注释。
> KAC-11 为报告自我纠正项，非缺陷。

## 关键修复详解

### KAC-05：BlinkCheck 为什么原来永远不触发

原实现只检查「单次移动事件位移 > 3.0 格」，存在双重真空区：

1. **超过 3.0 格**：Paper 内置 `moved too quickly!` 校验**先于插件生效且为丢弃式**，
   超限数据包根本到不了插件；
2. **低于 3.0 格**：不满足 `dist > max`，走 `decay()` 分支，永不计违规。

实测 33 次 flag 全部来自 `speed`，`blink` 一次未触发。

**v3.1 改为速度口径**：计算 `dist / elapsedTicks`（格/tick）。
即使位移被 Paper 截断到 4 格，只要发生在 1 tick 内，归一速度仍是 4.0 格/tick，
远高于人类上限（疾跑 0.28 / 跳跃加速 ≈0.4 / 鞘翅 ≈0.7）。

验证结果（7 个作弊样本）：

| 版本 | 检出率 | 误报 |
|---|---|---|
| v3.0 | 71% | 0 |
| v3.1 | **100%** | **0** |

高 ping 场景（300ms 下瞬移 10 格）在两版均能命中，正常高 ping 合包（1.40 格/5tick）不被误判。

### KAC-06：事件路由性能

原实现每个事件都遍历全部 28 个检测器，大量检测器只是空跑基类空实现，
且每个检测器还要独立调用 `isExempt`（内部最多 4 次权限查询）。
`PlayerMoveEvent` 作为最高频事件，每秒可触发数十次。

v3.1 引入 `eventMask()` 能力声明 + 事件索引：

| 事件 | v3.0 遍历 | v3.1 遍历 |
|---|---|---|
| move | 28 | **12** |
| dmg-dealt | 28 | 7 |
| dmg-taken | 28 | 3 |
| break | 28 | 3 |
| interact | 28 | 2 |
| 其余 6 类 | 28 | 1~2 |
| **合计** | **308** | **34（-89%）** |

同时对 `isExempt` 做 tick 级缓存：权限在 1 tick 内不会变化，
而原来每秒会产生数百次冗余权限查询。

### KAC-09：处罚链路修复

原实现只在配置值 `< 0` 时回落到全局默认，但 `config.yml` 里 28 个检测器
**全部显式写着 `kick-vl: 0`**，于是 `default-kick-vl: 20` 永不生效，
反作弊退化为纯告警器。

v3.1 语义修正：

- `kick-vl` 缺失或为 `0` → 回落 `punishments.default-kick-vl`
- `kick-vl: -1` → 显式关闭该检测的自动处罚（纯观察模式）

## 兼容性

- **配置兼容**：旧配置文件可直接使用。`blink.max-distance` 若仍填写旧的 `3.0`，
  插件会自动折算为 `1.6`。
- **协议兼容**：客户端 `KeranClient 2.0.0` 无需升级即可配套使用。
- **升级建议**：升级后建议执行 `/kac reload` 并检查 `punishments.default-kick-vl`
  是否符合预期，避免线上突然开始踢人。

## 验证

- 服务端：Paper 1.20.1 (git-Paper-196) 实测加载通过，28 个检测器注册正常
- 客户端：Fabric 1.20.1 构建通过，黑名单自测误报 0/20、检出 10/10
